# 仅允许已校准版式的高置信 PDF 诗歌自动公开

PDF 导入默认 dry-run，并保存文件校验值、PDF 页码、左右区域、书内页码、规则版本、内容指纹、置信等级与失败原因。每种新布局模板必须先由人工核对一首代表作；未校准模板、非分行诗歌、非完整公历日期、坐标与裁剪结果不一致、乱码、跨栏或重复冲突都不能自动公开。

通常自动生成的内容仍从 `ingested` 开始。“已校准模板＋高置信 PDF 抽取”是唯一允许直接成为 `verified` 的例外，因为人工审核的对象从每一首作品转移到了稳定版式模板和固定抽取规则。旧作仍按年份从新到旧逐批启用，以便每批独立部署和回退。
